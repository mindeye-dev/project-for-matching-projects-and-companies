# Shared Utilities

This directory is for shared models, schemas, and utility functions used by multiple components (backend, scrapers, etc).
 
## Usage
- Place common code here for import across the monorepo. 